<UserControl
    x:Class="ComicReader.Controls.ComicItemVertical"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ComicReader.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:muxm="using:Microsoft.UI.Xaml.Media"
    xmlns:data="using:ComicReader.Data"
    d:DataContext="{d:DesignInstance Type=data:SearchResultData}"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Background="#30FFFFFF"
          CornerRadius="6"
          PointerPressed="{x:Bind Ctx.OnItemPressed}">
        <Grid.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Text="Hide" Click="{x:Bind Ctx.OnHideClicked}">
                    <MenuFlyoutItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE8FF;"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuFlyout>
        </Grid.ContextFlyout>
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="220"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <MenuFlyoutItem/>
        <Grid Grid.Row="1" Padding="20,35">
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <winui:ProgressRing Visibility="{x:Bind Ctx.IsImageLoadedN}"/>
                <Grid Visibility="{x:Bind Ctx.IsImageLoaded}">
                    <Image Source="{x:Bind Ctx.Image}" Opacity="0"/>
                    <Border CornerRadius="6">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Ctx.Image}"
                                        Stretch="Uniform"/>
                        </Border.Background>
                    </Border>
                    <Border CornerRadius="5">
                        <Border.Background>
                            <muxm:AcrylicBrush BackgroundSource="Backdrop"
                                               TintOpacity="0.0"
                                               TintLuminosityOpacity="0.0"/>
                        </Border.Background>
                    </Border>
                    <FontIcon Glyph="&#xE735;"
                              FontSize="14"
                              Margin="0,0,10,10"
                              Foreground="SkyBlue"
                              VerticalAlignment="Bottom"
                              HorizontalAlignment="Right"
                              Visibility="{x:Bind Ctx.IsFavorite}"/>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="2" Padding="10">
            <StackPanel Grid.Row="0" Orientation="Vertical" Spacing="3">
                <TextBlock FontFamily="Segoe UI"
                           FontSize="14"
                           FontWeight="SemiBold"
                           MaxLines="2"
                           Text="{x:Bind Ctx.Title}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Viewbox HorizontalAlignment="Left"
                             VerticalAlignment="Center"
                             Height="20">
                        <RatingControl Margin="0,0,-13,0"
                                       PlaceholderValue="{x:Bind Ctx.Rating}"
                                       Style="{StaticResource RatingControlStyleReadOnly}"
                                       Visibility="{x:Bind Ctx.IsRatingVisible}"/>
                    </Viewbox>
                    <TextBlock Text="{x:Bind Ctx.Progress}"
                               FontSize="12"
                               Opacity="0.6"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
