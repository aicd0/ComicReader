<UserControl x:Class="ComicReader.Controls.ReaderPreviewImage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ComicReader.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid Width="{StaticResource ReaderPreviewImageWidth}"
          Height="{StaticResource ReaderPreviewImageHeight}">
        <Image Source="{x:Bind Ctx.ImageSource}"
               Stretch="Uniform" />
        <Grid HorizontalAlignment="Right"
              VerticalAlignment="Bottom"
              Background="White"
              Padding="5,2,5,5"
              Margin="0,0,8,8"
              CornerRadius="5"
              Opacity="0.9">
            <TextBlock Text="{x:Bind Ctx.Page}"
                       Foreground="Black"
                       FontWeight="Bold"/>
        </Grid>
    </Grid>
</UserControl>
