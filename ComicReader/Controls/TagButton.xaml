<UserControl x:Class="ComicReader.Controls.TagButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:ComicReader.Controls"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="100">

    <Button Style="{StaticResource ButtonRevealStyle}"
            Background="{ThemeResource TagButtonBackground}"
            BorderThickness="1.5"
            CornerRadius="13"
            Height="26"
            MinWidth="26"
            Padding="9,0,9,2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="13"
            FontWeight="Bold"
            Click="{x:Bind Ctx.OnClicked}"
            ToolTipService.ToolTip="{x:Bind Ctx.Tag}">
        <Button.Content>
            <TextBlock Text="{x:Bind Ctx.Tag}"
                       TextTrimming="CharacterEllipsis"/>
        </Button.Content>
    </Button>
</UserControl>